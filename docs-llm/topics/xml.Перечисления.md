# Перечисления

Одно и то же значение зовётся по-разному в контракте и в интерфейсе: в XML лежит `Single`, в конструкторе то же значение называется `БезПовторений`, а в выпадающем списке подписано «Без повторений». Колонка **В конструкторе** даёт имя, под которым значение живёт в конструкторе, и следом в кавычках — подпись из списка, если она отличается от имени. Ассистент отвечает пользователю про интерфейс, а читает контракт, поэтому здесь и то и другое.

### Kind — вид объекта

Допустимые значения атрибута `Kind` у элемента `Objects`:

`Справочники` · `Документы` · `БизнесПроцессы` · `Задачи` · `РегистрыБухгалтерии` · `РегистрыСведений` · `РегистрыНакопления` · `ПланыСчетов` · `ПланыВидовХарактеристик` · `Перечисления` · `Константы` · `КритерииОтбора` · `ЖурналыДокументов` · `ПланыОбмена` · `ПланыВидовРасчета` · `РегистрыРасчета` · `Перерасчеты` · `ВнешниеИсточникиДанных` · `НумераторыДокументов` · `Последовательности`

---

### Dataset.Type — тип набора

| Значение | В конструкторе | Описание |
|---|---|---|
| `FirstRow` | `ПерваяСтрока` «Первая строка» | Берётся только первая строка результата |
| `LastRow` | `ПоследняяСтрока` «Последняя строка» | Берётся только последняя строка |
| `Concatenation` | `СоединениеСтрок` «Соединение строк» | Строки объединяются в одно значение |
| `Collection` | `МногострочнаяКоллекция` «Многострочная коллекция» | Многострочная коллекция — цикл по каждой строке |

---

### Area.Method — способ вывода

| Значение | В конструкторе | Описание |
|---|---|---|
| `Single` | `БезПовторений` «Без повторений» | Вывести один раз, независимо от числа строк набора |
| `Collection` | `ПоСтрокамКоллекции` «По строкам коллекции» | Цикл по строкам связанного набора |
| `Header` | `header` «Верхний колонтитул» | Верхний колонтитул (повторяется на каждой странице) |
| `Footer` | `footer` «Нижний колонтитул» | Нижний колонтитул (повторяется на каждой странице) |
| `Wrap` | `ПереносСтроки` «Перенос строки» | Длинная строка разбивается по ширине |
| `Skip` | `НеВыводитьНаПечать` «Не выводить на печать [без вывода]» | Область остаётся в макете, но в печатную форму не попадает |
| `Column` | `ОбластьКолонки` «Область колонки [без вывода]» | Служебная область-шаблон колонки: сама не выводится |
| `ControlRow` | `КонтрольнаяСтрока` «Контрольная строка [служ: перенос строки]» | Служебная область переноса: по ней меряется, влезает ли текст |
| `WrapRow` | `СтрокаПереноса` «Строка переноса [служ: перенос строки]» | Служебная область переноса: ею выводится каждый перенесённый кусок |
| `Empty` | `ПустаяОбласть` «Пустая область [служ: перенос строки]» | Служебная область переноса: ею добиваются пустые строки блока |

Список в конструкторе зависит от формата макета: `Header` и `Footer` предлагаются только для DOCX, `Column` — только для табличного документа.

---

### Area.Settings — настройка вывода

| Значение | В конструкторе | Описание |
|---|---|---|
| `NewSection` | `НовыйРаздел` «Новый раздел» | Начать новый раздел (разрыв страницы перед областью) |
| `RepeatInHeader` | `ПовторятьПриПечати` «Повторять в шапке» | Повторять в шапке каждой страницы |
| `RepeatInFooter` | `ПовторятьВПодвале` «Повторять в подвале» | Повторять в подвале каждой страницы |
| `CheckPrint` | `ПроверитьВывод` «Проверить вывод» | Проверять необходимость вывода перед выводом области |

---

### Filter.Item.ComparisonTypes — виды сравнения

Используется в `Area.OutputRule/ComparisonType`. Имена и состав соответствуют типовому 1С-перечислению `ВидСравненияКомпоновкиДанных` (`DataCompositionComparisonType`). Отдельной колонки «В конструкторе» здесь нет: конструктор хранит те же имена, что и XML, а подпись в списке совпадает с описанием ниже.

| Значение | Описание | Применимость по типу | Требует `<RightValue>` |
|---|---|---|---|
| `Equal` | Равно | любой | да |
| `NotEqual` | Не равно | любой | да |
| `Less` | Меньше | Число / Дата / Строка / Булево | да |
| `LessOrEqual` | Меньше или равно | Число / Дата / Строка / Булево | да |
| `Greater` | Больше | Число / Дата / Строка / Булево | да |
| `GreaterOrEqual` | Больше или равно | Число / Дата / Строка / Булево | да |
| `InList` | В списке | любой | да (список) |
| `NotInList` | Не в списке | любой | да (список) |
| `InHierarchy` | В иерархии | иерархические ссылочные | да |
| `NotInHierarchy` | Не в иерархии | иерархические ссылочные | да |
| `InListByHierarchy` | В списке по иерархии | иерархические ссылочные | да (список) |
| `NotInListByHierarchy` | Не в списке по иерархии | иерархические ссылочные | да (список) |
| `Contains` | Содержит | Строка | да |
| `NotContains` | Не содержит | Строка | да |
| `Filled` | Заполнено | любой | нет |
| `NotFilled` | Не заполнено | любой | нет |
| `Like` | Подобно (шаблон `%`, `_`) | Строка | да |
| `NotLike` | Не подобно | Строка | да |
| `BeginsWith` | Начинается с | Строка | да |
| `NotBeginsWith` | Не начинается с | Строка | да |

Применимость по типу — для фильтрации в UI редактора. На исполнении несовместимые комбинации трактуются как невыполненное правило.

---

### QRCode.Type — тип QR-кода

| Значение | В конструкторе | Описание |
|---|---|---|
| `Bank` | `УФЭБС` | УФЭБС (стандарт быстрого платежа) |
| `XML` | `XML` | Произвольный XML |
| `JSON` | `JSON` | Произвольный JSON |
| `Algorithm` | `Алгоритм` | Произвольный алгоритм на 1С |

---

### Event.Name — события жизненного цикла

| Значение | В конструкторе | Когда срабатывает |
|---|---|---|
| `BeforeInitialization` | `ПередИнициализацией` «Перед инициализацией» | Перед инициализацией макета |
| `OnDataReceiving` | `ПриПолученииДанных` «При получении данных» | При получении данных |
| `BeforeFormation` | `ПередФормированием` «Перед формированием» | Перед началом формирования |
| `BeforePageOutput` | `ПередВыводомСтраницы` «Перед выводом страницы» | Перед выводом каждой страницы |
| `BeforeAreaOutput` | `ПередВыводомОбласти` «Перед выводом области» | Перед выводом каждой области |
| `AfterAreaOutput` | `ПослеВыводаОбласти` «После вывода области» | После вывода области |
| `AfterPageOutput` | `ПослеВыводаСтраницы` «После вывода страницы» | После вывода страницы |
| `AfterFormation` | `ПослеФормирования` «После формирования» | После завершения формирования |

---

### Query.ResultType — тип результата запроса

| Значение | В конструкторе | Описание |
|---|---|---|
| `ValueTable` | `ТаблицаЗначений` «Таблица значений» | Обычный запрос, результат используется как набор данных |
| `TempTable` | `ВременнаяТаблица` «Временная таблица» | Временная таблица — промежуточный результат для других запросов |

---

### Parameter.Type — тип входного параметра

| Значение | В конструкторе | Описание |
|---|---|---|
| `Value` | «Значение» | Единственное значение |
| `List` | «Список» | Список значений |
| `Table` | «Таблица» | Таблица значений |
| `Algorithm` | «Алгоритм» | Вычисляется алгоритмом |

---

### Field.Dataset.Functions — функции дат

Применяются к полям типа `Дата` через атрибут `IsFunction = true` и элемент `<FunctionName>`. Конструктор хранит те же имена, что и XML, а подписью в списке служит описание из таблицы («Начало дня», «День недели» и так далее):

| Значение | Описание |
|---|---|
| `BegOfMinute` / `EndOfMinute` | Начало / конец минуты |
| `BegOfHour` / `EndOfHour` | Начало / конец часа |
| `BegOfDay` / `EndOfDay` | Начало / конец дня |
| `BegOfWeek` / `EndOfWeek` | Начало / конец недели |
| `BegOfMonth` / `EndOfMonth` | Начало / конец месяца |
| `BegOfQuarter` / `EndOfQuarter` | Начало / конец квартала |
| `BegOfYear` / `EndOfYear` | Начало / конец года |
| `Second` | Секунда |
| `Minute` | Минута |
| `Hour` | Час |
| `Day` | День месяца |
| `WeekDay` | День недели (1 = понедельник) |
| `DayOfYear` | День года |
| `WeekOfYear` | Неделя года |
| `Month` | Месяц |
| `Year` | Год |
